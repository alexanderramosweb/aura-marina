---
import "./Button.css";

interface Props {
  href: string;
  variant?: "primary";
}

const { href, variant = "primary" } = Astro.props;
---

<a href={href} class={`button button--${variant}`}>
  <slot />
</a>
